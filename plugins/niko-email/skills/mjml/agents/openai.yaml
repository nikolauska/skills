interface:
  display_name: "MJML"
  short_description: "Build and compile responsive email templates"
  default_prompt: "Use $mjml to create or update this MJML email template and compile it with strict validation."
