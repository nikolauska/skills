# Detection Reference

Files to scan in Step 1, organized by category. Glob to detect existence; read to extract details (skip binary files).

| Group | Files to detect |
|-------|----------------|
| Build & Runtime | **Lock:** `package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`, `bun.lockb`, `uv.lock`, `poetry.lock`, `Cargo.lock`, `go.sum`, `Gemfile.lock`, `composer.lock`, `mix.lock`, `packages.lock.json`, `stack.yaml.lock`, `cabal.project.freeze` . **Config:** `package.json`, `pyproject.toml`, `Cargo.toml`, `go.mod`, `deno.json`, `Makefile`, `Justfile`, `Taskfile.yml`, `build.gradle`, `mix.exs`, `*.csproj`, `pom.xml`, `*.sln`, `global.json`, `Directory.Build.props`, `build.gradle.kts`, `settings.gradle`, `project.clj`, `deps.edn`, `bb.edn`, `shadow-cljs.edn`, `build.sbt`, `build.mill`, `stack.yaml`, `*.cabal`, `cabal.project`, `package.yaml` . **Versions:** `.nvmrc`, `.python-version`, `.tool-versions`, `.mise.toml`, `rust-toolchain.toml`, `volta` in `package.json` . **Monorepo:** `turbo.json`, `nx.json`, `pnpm-workspace.yaml`, `workspaces` in `package.json`, `[workspace]` in `Cargo.toml` |
| Quality & Hooks | **Lint/format:** `biome.json`, `.eslintrc*`, `.prettierrc*`, `clippy.toml`, `.golangci.yml`, `.editorconfig`, `ruff` in deps, `tsconfig.json` (strict, aliases), `checkstyle.xml`, `.cljfmt.edn`, `.clj-kondo/`, `.scalafmt.conf`, `.scalafix.conf`, `.hlint.yaml`, `.ormolu` . **Test:** `jest.config.*`, `vitest.config.*`, `pytest.ini`, `[tool.pytest]`, `.rspec`, `phpunit.xml*` . **Hooks:** `.husky/`, `lint-staged*`, `.pre-commit-config.yaml`, `lefthook.yml` |
| Infra & Deploy | **CI:** `.github/workflows/`, `.gitlab-ci.yml`, `Jenkinsfile`, `.circleci/` . **Containers:** `Dockerfile`, `docker-compose.y*` . **Dev env:** `flake.nix`, `shell.nix`, `.envrc`, `.devcontainer/` . **IaC/Serverless/K8s:** `template.yaml` (SAM), `serverless.y*`, `*.tf`, `cdk.json`, `pulumi.*`, `wrangler.toml`, `sst.config.*`, `Chart.yaml`, `kustomization.yaml`, `skaffold.yaml`, `Tiltfile` |
| Data & APIs | **Schemas:** `openapi.y*`, `swagger.y*`, `schema.graphql`, `*.proto`, `buf.yaml` . **Codegen:** grep scripts/config for `openapi-generator`, `graphql-codegen`, `protoc`, `buf generate`, `orval`, `swagger-codegen` . **Env templates:** `.env.example`, `.env.template`, `.env.sample`, `.env.local.example` |
| Security & Observability | **Security/registry:** `.gitignore`, `.npmrc`, `.pypirc`, `NuGet.config`; grep CI for `npm audit`, `cargo audit`, `pip-audit`, `snyk`, `trivy`, `grype` . **CI secrets:** grep CI config for `secrets.`, `vault`, `aws-actions/configure-aws-credentials`, `OIDC` . **Dep updates:** `renovate.json*`, `.github/dependabot.yml` . **Observability:** grep deps for `@sentry/*`, `sentry-sdk`, `dd-trace`, `@opentelemetry/*`, `pino`, `structlog`, `tracing` (Rust) |
| Docs & Meta | **Git:** `CODEOWNERS`, `.github/PULL_REQUEST_TEMPLATE*`, `CONTRIBUTING.md`, `.gitmodules` . **Docs:** `.storybook/`, `docusaurus.config.*`, `mkdocs.yml`, `docs/adr/`, `docs/decisions/`, `docs/rfcs/` . **License:** `LICENSE`, `LICENSE.*`, `COPYING`, `SPDX-License-Identifier` in build config |
